Add Rows and Columns in NumPy Array

import numpy as np
array = np.array([10, 20, 30, 40, 50])
array2d = np.array([[1, 2], [3, 4]])
added_row = np.append(array2d, [[5, 6]], axis=0)
added_column = np.append(array2d, [[7], [8]], axis=1)
print("Array with added row:\n", added_row)
print("Array with added column:\n", added_column)

